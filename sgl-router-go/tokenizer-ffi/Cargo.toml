[package]
name = "tokenizer-ffi"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
tokenizers = "0.22.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
anyhow = "1.0"

# C FFI bindings
[build-dependencies]
cbindgen = "0.26"

