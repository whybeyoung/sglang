// Quick test to check tokenizer decode API
use tokenizers::tokenizer::Tokenizer;

fn main() {
    // Check if tokenizer.decode accepts Vec<u32>
    // This is just to verify the API signature
    println!("Tokenizer decode method signature check");
}
